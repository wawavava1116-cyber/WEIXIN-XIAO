<view class="safe-page profile-page">
  <view class="profile-hero glass">
    <view class="profile-avatar">
      <image wx:if="{{profile.avatarUrl}}" src="{{profile.avatarUrl}}" mode="aspectFill"></image>
      <text wx:else>人</text>
    </view>
    <view class="profile-copy">
      <text class="profile-name">{{profile.nickname}}</text>
      <text class="profile-mode">{{profile.modeText}}</text>
    </view>
  </view>

  <view class="profile-card glass">
    <text class="profile-card-title">账号状态</text>
    <text class="profile-card-text">{{profile.hasProfile ? '已保存微信昵称。' : '当前资料未完善。点击后将打开微信提供的昵称选择页；头像可不提供。'}}</text>
    <button wx:if="{{profile.canCompleteProfile && !showProfileForm}}" class="profile-login-button" loading="{{savingProfile}}" bindtap="showManualProfileForm">选择微信昵称</button>
    <view wx:if="{{showProfileForm}}" class="wechat-profile-form">
      <text class="profile-card-text">请选择或填写微信昵称。</text>
      <input class="nickname-input" type="nickname" placeholder="选择或确认微信名" value="{{profileNickname}}" bindinput="onProfileNicknameInput" bindchange="onProfileNicknameInput" />
      <button class="profile-login-button" loading="{{savingProfile}}" bindtap="saveWechatProfile">保存资料</button>
    </view>
  </view>

  <view class="profile-card glass">
    <text class="profile-card-title">预测小组奖牌</text>
    <view class="medal-grid">
      <view class="medal-cell gold">
        <text>金牌</text>
        <text>{{medals.gold || 0}}</text>
      </view>
      <view class="medal-cell silver">
        <text>银牌</text>
        <text>{{medals.silver || 0}}</text>
      </view>
      <view class="medal-cell bronze">
        <text>铜牌</text>
        <text>{{medals.bronze || 0}}</text>
      </view>
    </view>
  </view>

  <view class="profile-card glass">
    <text class="profile-card-title">使用说明</text>
    <text class="profile-card-text">用户数据仅用于区分游客和微信昵称用户；赛程、预测和复盘内容不因账号状态改变。</text>
  </view>
</view>
