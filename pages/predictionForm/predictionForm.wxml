<view class="safe-page prediction-form-page">
  <view class="match-hero glass" wx:if="{{match}}">
    <view class="match-team">
      <image class="match-flag" src="{{match.home.flag}}" mode="aspectFit"></image>
      <text class="match-abbr">{{match.home.abbr}}</text>
      <text class="match-name">{{match.home.cn}}</text>
    </view>
    <view class="match-center">
      <text>{{match.dateText}}</text>
      <text>{{match.kickoff}}</text>
      <text>VS</text>
    </view>
    <view class="match-team right">
      <image class="match-flag" src="{{match.away.flag}}" mode="aspectFit"></image>
      <text class="match-abbr">{{match.away.abbr}}</text>
      <text class="match-name">{{match.away.cn}}</text>
    </view>
  </view>

  <view class="pick-section">
    <text class="pick-title">胜平负</text>
    <view class="option-row">
      <block wx:for="{{resultOptions}}" wx:key="value">
        <button class="pick-chip {{item.selected ? 'selected' : ''}}" data-value="{{item.value}}" bindtap="selectResult">{{item.value}}</button>
      </block>
    </view>
  </view>

  <view class="pick-section">
    <text class="pick-title">备选胜平负</text>
    <text class="pick-note">可选</text>
    <view class="option-row">
      <block wx:for="{{backupOptions}}" wx:key="value">
        <button class="pick-chip {{item.selected ? 'selected' : ''}}" data-value="{{item.value}}" bindtap="selectBackup">{{item.value}}</button>
      </block>
    </view>
  </view>

  <view class="pick-section">
    <view class="section-head">
      <text class="pick-title">比分</text>
      <text class="pick-note">选择 2 个</text>
    </view>
    <block wx:for="{{scoreGroups}}" wx:key="key">
      <view class="score-group">
        <text class="score-group-title">{{item.title}}</text>
        <view class="score-grid">
          <block wx:for="{{item.options}}" wx:for-item="score" wx:key="value">
            <button class="score-card {{score.selected ? 'selected' : ''}}" data-value="{{score.value}}" bindtap="selectScore">
              <text>{{score.value}}</text>
            </button>
          </block>
        </view>
      </view>
    </block>
  </view>

  <view class="pick-section">
    <text class="pick-title">进球数</text>
    <view class="goal-grid">
      <block wx:for="{{goalOptions}}" wx:key="value">
        <button class="pick-chip goal-chip {{item.selected ? 'selected' : ''}}" data-value="{{item.value}}" bindtap="selectGoals">{{item.value}}</button>
      </block>
    </view>
  </view>

  <button class="submit-button" bindtap="submitPrediction">提交预测</button>
</view>
