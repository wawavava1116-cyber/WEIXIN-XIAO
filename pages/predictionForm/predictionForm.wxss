.prediction-form-page {
  padding-bottom: 18rpx;
}

.match-hero {
  display: grid;
  grid-template-columns: 1fr 86rpx 1fr;
  align-items: center;
  gap: 8rpx;
  min-height: 118rpx;
  padding: 12rpx 14rpx;
  border-radius: 14rpx;
}

.match-team {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  min-width: 0;
}

.match-team.right {
  align-items: flex-end;
}

.match-flag {
  width: 44rpx;
  height: 30rpx;
}

.match-abbr {
  margin-top: 5rpx;
  color: #fff;
  font-size: 28rpx;
  line-height: 1;
  font-weight: 1000;
}

.match-name {
  max-width: 100%;
  margin-top: 4rpx;
  color: rgba(255, 255, 255, 0.62);
  font-size: 16rpx;
  line-height: 1.2;
  font-weight: 900;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.match-center {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2rpx;
  color: rgba(255, 255, 255, 0.68);
  font-size: 16rpx;
  line-height: 1.2;
  font-weight: 900;
  text-align: center;
}

.match-center text:last-child {
  color: #d6aa4c;
  font-size: 24rpx;
  font-weight: 1000;
}

.pick-section {
  margin-top: 12rpx;
}

.section-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
}

.pick-title,
.pick-note,
.score-group-title {
  display: inline-block;
}

.pick-title {
  margin: 0 4rpx 7rpx;
  color: #fff;
  font-size: 24rpx;
  font-weight: 1000;
}

.pick-note {
  margin-left: 8rpx;
  color: rgba(214, 170, 76, 0.82);
  font-size: 16rpx;
  font-weight: 900;
}

.option-row,
.goal-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 7rpx;
}

.goal-grid {
  grid-template-columns: repeat(6, 1fr);
}

.pick-chip {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 52rpx;
  border: 2rpx solid rgba(214, 170, 76, 0.24);
  border-radius: 9rpx;
  color: #fff;
  background: #141414;
  font-size: 22rpx;
  font-weight: 1000;
}

.goal-chip {
  min-height: 46rpx;
  font-size: 19rpx;
}

.score-group {
  margin-top: 7rpx;
}

.score-group-title {
  margin: 0 4rpx 5rpx;
  color: rgba(255, 255, 255, 0.62);
  font-size: 16rpx;
  font-weight: 900;
}

.score-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 6rpx;
}

.score-card {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 46rpx;
  padding: 3rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.12);
  border-radius: 8rpx;
  color: #fff;
  background: #151515;
  font-size: 20rpx;
  line-height: 1;
  font-weight: 1000;
}

.pick-chip.selected,
.score-card.selected {
  color: #050505;
  border-color: #d6aa4c;
  background: #d6aa4c;
}

.submit-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  min-height: 58rpx;
  margin-top: 14rpx;
  border-radius: 10rpx;
  color: #050505;
  background: #d6aa4c;
  font-size: 24rpx;
  font-weight: 1000;
}
